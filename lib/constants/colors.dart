import 'package:flutter/material.dart';

class AppColors {
  // Main theme colors
  static const Color primary = Color(0xFFEAD78D); // Muted yellow (theme color)
  static const Color secondary = Color(0xFF72B5B7); // Teal accent
  static const Color tertiary = Color(0xFFE5855E); // Coral accent

  // Neutral colors
  static const Color background = Colors.white;
  static const Color cardBackground = Colors.white;
  static const Color scaffoldBackground = Color(0xFFF8F8F8);

  // Text colors
  static const Color textPrimary = Color(0xFF303030);
  static const Color textSecondary = Color(0xFF606060);
  static const Color textLight = Color(0xFF909090);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // Social action colors
  static const Color like = Color(0xFFE91E63);
  static const Color comment = Color(0xFF2196F3);
  static const Color share = Color(0xFF4CAF50);

  // Button colors
  static const Color buttonPrimary = primary;
  static const Color buttonSecondary = Color(0xFF757575);

  // Divider colors
  static const Color divider = Color(0xFFE0E0E0);

  // Shadow colors
  static const Color shadow = Color(0x40000000);
}